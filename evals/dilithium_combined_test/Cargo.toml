[package]
name = "dilithium_combined_test"
version = "0.1.0"
edition = "2024"

[dependencies]
dilithium-rs = { version = "0.2.0", features = ["simd"]} 
env = "1.0.1"
useful_stats = { path = "../useful_stats" }
